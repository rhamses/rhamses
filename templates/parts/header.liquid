<header class="site-header">
  <a href="{{ site.home_url }}" class="site-brand">Rhamses</a>
  <div class="site-header-actions">
    <form class="site-search" action="{{ site.locale_prefix }}/search" method="get" role="search">
      <label class="visually-hidden" for="search-field">{% if route.locale == 'en' %}Search{% else %}Buscar{% endif %}</label>
      <input
        id="search-field"
        type="search"
        name="q"
        value="{{ search.query }}"
        placeholder="{% if route.locale == 'en' %}Search…{% else %}Buscar…{% endif %}"
      />
    </form>
    <div class="site-header-actions-right">
    <nav class="site-lang-switcher" aria-label="Language">
      {% for item in locale_switcher %}
        <a
          href="{{ item.url }}"
          class="site-lang-link{% if item.active %} is-active{% endif %}"
          hreflang="{{ item.code }}"
          aria-label="{{ item.label }}"
        >{{ item.flag }}</a>
      {% endfor %}
    </nav>
    <div class="theme-toggle" aria-label="Theme">
      <label class="{% if route.locale == 'en' %}checked{% endif %}" title="Light theme">
        <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true">
          <path fill-rule="evenodd" d="M10 2a1 1 0 011 1v1a1 1 0 11-2 0V3a1 1 0 011-1zm4 8a4 4 0 11-8 0 4 4 0 018 0zm-.464 4.95l.707.707a1 1 0 001.414-1.414l-.707-.707a1 1 0 00-1.414 1.414zm2.12-10.607a1 1 0 010 1.414l-.706.707a1 1 0 11-1.414-1.414l.707-.707a1 1 0 011.414 0zM17 11a1 1 0 100-2h-1a1 1 0 100 2h1zm-7 4a1 1 0 011 1v1a1 1 0 11-2 0v-1a1 1 0 011-1zM5.05 6.464A1 1 0 106.465 5.05l-.708-.707a1 1 0 00-1.414 1.414l.707.707zm1.414 8.486l-.707.707a1 1 0 01-1.414-1.414l.707-.707a1 1 0 011.414 1.414zM4 11a1 1 0 100-2H3a1 1 0 000 2h1z" clip-rule="evenodd"/>
        </svg>
        <input type="radio" name="theme-toggle" value="light" />
      </label>
      <label title="Dark theme">
        <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true">
          <path d="M17.293 13.293A8 8 0 016.707 2.707a8.001 8.001 0 1010.586 10.586z"/>
        </svg>
        <input type="radio" name="theme-toggle" value="dark" />
      </label>
    </div>
    </div>
  </div>
</header>
